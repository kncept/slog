import { AwsAccessKeyProperties, HostnameProperties } from "./orchestration/env-properties"

// add as many property types from orchestration/properties as required
const properties : AwsAccessKeyProperties & HostnameProperties & BlogProperties = {
    publicUrl: 'https://',
    reactAppApiEndpoint: 'https://',
    adminUsers: [], // 'ProviderId:UserId'

    awsAccessKeyId: ,
    awsSecretAccessKey: ,
    awsRegion: '

    loginProviders: [
    ]

}

export default properties